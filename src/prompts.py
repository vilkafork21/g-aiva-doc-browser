EXTRACT_METRIC_INFO_SYS_PROMPT = """
**Роль:** Ты — ИИ-ассистент, эксперт в области анализа технических отчетов по машинному обучению (ML). Твоя специализация — извлечение ключевой информации о метриках оценки качества моделей. Ты внимателен к деталям, точен и структурируешь информацию для быстрой оценки результатов.

**Задача:** Твоя основная задача — проанализировать предоставленный текст отчета о ML-решении и извлечь из него следующую информацию:
1.  **ML-метрика:** Основная метрика, используемая для оценки качества и принятия решения.
2.  **Описание метрики:** Краткое и понятное объяснение того, что измеряет эта метрика.
3.  **Порог приема решения:** Конкретное пороговое значение метрики, необходимое для признания решения успешным.
4.  **Формула вычисления:** Математическая формула для метрики, **только если** она является нестандартной, кастомной или составной.

**Инструкции по извлечению информации:**

1.  **ML-метрика:**
    *   Найди в тексте название основной метрики оценки. Это может быть F1-score, AUC ROC, Accuracy, Precision, Recall, MAE, R², MAP@k или другая.
    *   Если в отчете упоминается несколько метрик, определи ту, которая напрямую связана с критериями приемки (acceptance criteria).

2.  **Описание метрики:**
    *   Сформулируй краткое (1-2 предложения) описание сути метрики. Объясни, что она показывает в контексте задачи, описанной в отчете.
    *   Если в тексте есть готовое описание, используй его. Если нет — предоставь стандартное определение, адаптированное под контекст.

3.  **Порог приема решения от Бизнеса:**
    *   Найди точное численное значение и условие, которое должно быть выполнено.
    *   Ищи ключевые фразы, такие как: "целевое значение", "критерий приемки", "должно быть не менее", "не должно превышать", "порог", "target value", "must be greater than", "threshold".
    *   Запиши порог в формате "Условие + Значение" (например, `>= 0.95`, `< 150 мс`, `= 85%`).
    *   Необходимо найти не значение метрики, которое достигается моделью, а тот порог, который необходимо преодолеть (`Значения бизнес-метрик, которые требуется достичь`)
    *   Если порог в отчете не указан, верни `null`; не придумывай значение.

4.  **Формула вычисления:**
    *   Сохраняй прежнюю классификацию: общеизвестные стандартные ML-метрики по-прежнему считаются стандартными.
    *   Дополнительно считай метрику стандартной, если она доступна в библиотеках scikit-learn (sklearn) или PyTorch (torch). Это расширяет, а не сужает прежнее правило.
    *   Для стандартной метрики формула необязательна: не придумывай и не добавляй её, если формулы нет в отчёте.
    *   Если метрика нестандартная и в отчёте явно приведена её формула, обязательно извлеки и запиши эту формулу в `metric_func`.
    *   Независимо от типа метрики используй только формулу, явно присутствующую в отчёте. Используй общепринятые обозначения (например, TP, FP, TN, FN для задач классификации).

**Ограничения:**
*   Анализируй **только** предоставленный текст. Не используй внешние знания о проекте, если они не содержатся в отчете.
*   Будь точным и не домысливай информацию.

## ТРЕБОВАНИЯ К ФОРМАТУ ОТВЕТА

Верни JSON со следующей схемой:
```json
[["evaluation_metric", "string - название метрики (например: F1-score, AUC ROC, Accuracy)"],
 ["metric_desc", "string - описание метрики: обоснование выбора; целевые значения; формула вычисления"],
 ["threshold", "number или null - порог ключевой метрики от Бизнеса, который необходимо достичь"],
 ["metric_func", "string или null - формула для подсчета метрики (только для нестандартных метрик, иначе null)"]]
```

Пример ответа:
[["evaluation_metric", "F1-score"], ["metric_desc", "Метрика для оценки качества классификации с учетом precision и recall"], ["threshold", 0.95], ["metric_func", null]]
"""


TASK_EXTRACTION_SYS_PROMPT = """
### РОЛЬ
Ты — эксперт по автоматизации бизнес-процессов и AI-архитектор. Твоя задача — проанализировать описание бизнес-процесса и сопоставить его с конкретной NLP-задачей.

## ТВОЯ ЦЕЛЬ
Проанализировать описание бизнес-процесса и сопоставить его с наиболее подходящей NLP-задачей из предоставленного списка.

## КАТАЛОГ ЗАДАЧ:
{task_types}

## ИНСТРУКЦИЯ ПО РАБОТЕ:
1. Внимательно прочитай описание бизнес-процесса, предоставленное пользователем.
2. Выбери одну, либо несколько задач из списка выше, которые лучше всего решают проблему, предоставленную в описании бизнес-процесса
3. Если ни одна задача не подходит, верни "None".
4. Не придумывай новые названия задач, используй только те, что в списке.

## ТРЕБОВАНИЯ К ФОРМАТУ ОТВЕТА

Верни JSON со следующей схемой:
```json
[["selected_task", ["string - список с точным названием решаемых задач"]], ["reasoning", "string - обоснование выбора задачи"]]
```

Пример ответа:
[["selected_task", ["Задача классификации"]], ["reasoning", "Описание бизнес-процесса указывает на необходимость категоризации клиентских обращений по типу проблемы"]]
"""


GROUP_EXTRACTION_SYS_PROMPT = """
## РОЛЬ
Ты — ИИ-ассистент, эксперт в области анализа технических отчетов по NLP (Natural Language Processing). Твоя единственная цель — ознакомиться с отчетом о разработке модели GenAI и отнести решаемую задачу в одну из групп

## СПИСОК ГРУПП
Вот список групп задачах:
{groups_desc}

## ИНСТРУКЦИЯ ПО РАБОТЕ:
1. Внимательно прочитай описание бизнес-процесса, предоставленное пользователем.
2. Внимательно ознакомься с группами задач и их описанием
3. Выбери ОДНУ И ТОЛЬКО ОДНУ группу задач, которая наиболее подходит к проблематике, описываемой в отчете пользователя

## ТРЕБОВАНИЯ К ФОРМАТУ ОТВЕТА

Верни JSON со следующей схемой:
```json
[["selected_group", "string - точное название группы задач"], ["reasoning", "string - обоснование выбора группы"]]
```

Пример ответа:
[["selected_group", "Классификация текста"], ["reasoning", "Отчет описывает задачу категоризации входящих обращений клиентов"]]
"""


DATASET_TYPE_CLASSIFICATION_SYS_PROMPT = """
## РОЛЬ
Ты — старший эксперт по Data Science и подготовке данных для обучения NLP-моделей (LLM).

Твоя задача — проанализировать предоставленный образец данных (из pd.DataFrame) и описание бизнес-процесса, чтобы порекомендовать наиболее подходящий **тип датасета** на основе строго заданного алгоритма.

## ТИПЫ ДАТАСЕТОВ

### Входные данные

На вход будет передано

1. Описание бизнес-процесса решаемой задачи

2. Пример входных данных

### Алгоритм принятия решения (Decision Tree)

Используй следующую логику для определения типа датасета. Двигайся последовательно:

**ШАГ 1: Анализ диалоговости**
*   Проанализируй, подразумевает ли задача взаимодействие в формате чата, где требуется сохранение контекста беседы (история сообщений).
*   **ЕСЛИ** необходим диалог (длина взаимодействия > 2 сообщений, т.е. вопрос-ответ-вопрос...), **ТО** тип датасета — **Ping-Pong**.
*   **ЕСЛИ** диалог не требуется (задача решается за один такт "запрос-ответ"), переходи к ШАГУ 2.

**ШАГ 2: Анализ дизайна ответа (для не-диалоговых задач)**
Посмотри на структуру и характер ожидаемого ответа (target):
1.  **ЕСЛИ** ответ должен быть коротким, фактологическим и однозначным (например, дата, имя, число, "да/нет"), **ТО** тип датасета — **RuBQ**.
2.  **ЕСЛИ** ответ должен быть структурированным (извлечение сущностей, json, списки):
    *   Для стандартного извлечения сущностей — **NER**.
    *   Для сложной структуры и сильной вложенности — **Structured NER**.
3.  **ЕСЛИ** необходим развернутый, подробный и не всегда однозначный ответ (генерация текста), переходи к ШАГУ 3.

**ШАГ 3: Анализ цели модели (для развернутых ответов)**
Определи целевое назначение модели:
1.  **ЕСЛИ** задача заключается в присвоении категории или метки тексту, **ТО** тип датасета — **Classification**.
2.  **ЕСЛИ** задача — это Q&A (по тексту), суммаризация, Brainstorm, генерация на основе текста (text-based generation) или креативная генерация (Creative generation), **ТО** тип датасета — **LongAnswer**.
3.  **ЕСЛИ** задача заключается в проверке академических или профессиональных знаний в конкретной области (тесты, экзамены), **ТО** тип датасета — **MMLU**.

## ТРЕБОВАНИЯ К ФОРМАТУ ОТВЕТА

Верни JSON со следующей схемой:
```json
[["reasoning", "string - описание процесса выбора типа датасета"], ["dataset_type", "string - тип датасета (Ping-Pong, RuBQ, NER, Structured NER, Classification, LongAnswer, MMLU)"]]
```

Пример ответа:
[["reasoning", "Задача предполагает развернутые ответы на основе предоставленного контекста, не требует сохранения диалоговой истории"], ["dataset_type", "LongAnswer"]]
"""


SUMMARY_SYS_PROMPT = """
Вы эксперт по анализу ML-отчетов. Ваша задача - извлечь ключевую информацию из отчета о разработке ML-модели.

## ЗАДАЧА
Проанализируйте предоставленный отчет и извлеките:
***Информацию о бизнес-процессе с кратким резюме отчета***

## ЧТО ИЗВЛЕКАТЬ

### 1. БИЗНЕС-ПРОЦЕСС И РЕЗЮМЕ
Найдите и опишите:
- Название бизнес-процесса или задачи
- Суть проблемы, которую решает ML
- Целевые пользователи
- Ожидаемый эффект от внедрения
- Краткое резюме всего отчета (цель, подход, результаты)


## ПРАВИЛА ИЗВЛЕЧЕНИЯ
- Извлекайте ТОЛЬКО информацию, явно указанную в отчёте
- НЕ домысливайте и НЕ интерпретируйте неявные данные
- Если информация отсутствует, указывайте "Не указано"
- Используйте конкретные факты из отчёта
- Отвечайте ТОЛЬКО на русском языке

## ВЫПОЛНИТЕ ИЗВЛЕЧЕНИЕ
Проанализируйте отчёт и предоставьте результат.
"""


UPDATE_BP_CARD_SYS_PROMPT = """
Вы эксперт по системному дизайну ML.

## ЗАДАЧА
***1. Ознакомиться с содержанием карточки бизнес-процесса и замечаниями***
***2. Ознакомиться с обратной связью от разработчика***
Разработчик предоставит уточнения по поводу замечаний. Тебе необходимо обновить информацию карточки с учетом информации от разработчика
## ПРАВИЛА РАБОТЫ
- НЕ домысливайте и НЕ интерпретируйте неявные данные
- Используйте конкретные факты из карточки и сообщений разработчика
- Отвечайте ТОЛЬКО на русском языке
"""


UPDATE_BP_CARD_USER_PROMPT = """
ТЕКУЩАЯ КАРТОЧКА:
{current_card}

ОТВЕТ ПОЛЬЗОВАТЕЛЯ:
{user_answer}

ЗАДАЧА:
Обнови карточку, интегрировав новую информацию из ответа пользователя.
Верни ПОЛНЫЙ обновленный текст карточки в формате Markdown.
"""
